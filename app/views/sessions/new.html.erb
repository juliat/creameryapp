<div id="login-form-box">
    <h1>Log In</h1>

    <%= form_tag sessions_path do %>
        <div class="control-group">
        <%= label_tag :email %>
        <div class="controls">
          <%= text_field_tag :email, params[:email], :placeholder => "Email Address" %>
        </div>
      </div>

        <div class="control-group">
        <%= label_tag :password %>
        <div class="controls">
          <%= password_field_tag :password %>
        </div>
      </div>
      
     <div class="field">
        <%= check_box_tag :remember_me, 1, params[:remember_me] %>
        <%= label_tag :remember_me %>
     </div>

      <%= submit_tag("Log In", :class => "btn btn-primary") %>
      
      <a><%= link_to "Forgot your password?", new_password_reset_path, :id => "resetPassword" %></a>
      
    <% end %>
    <hr>
    <p>To see the application as an administrator, login as alex@example.com.</p>
    <p>To see the application as a store manager, login as rachel@example.com.</p> 
    <p>To see the application as an employee, login as dusty@example.com. </p>
    <p>The password for all users is creamery.</p>
</div>

